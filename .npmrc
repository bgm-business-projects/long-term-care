# pnpm 10+ 的 ignored-builds 預設會 error 出來，CI/Docker 場景關掉
# 我們的 build 不依賴 esbuild/@parcel/watcher 的 postinstall（Nuxt build 自帶 prebuilt binary）
ignored-builds-check=false

# 略過 peer-dep 嚴格檢查（與既有專案行為一致）
strict-peer-dependencies=false

# 自動安裝 peer dependencies
auto-install-peers=true
